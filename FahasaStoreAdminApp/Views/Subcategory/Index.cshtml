@using FahasaStoreAPI.Models.EntitiesModels
@model IEnumerable<FahasaStoreAPI.Models.EntitiesModels.SubcategoryEntities>

@{
    ViewData["Title"] = "Quản lý Thể loại con";
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["ActiveSidebar"] = 4;
    ViewData["UrlCurrent"] = "/Subcategory/Index";
    var categories = ViewData["Categories"] as List<CategoryEntities>;
}
<div class="d-flex justify-content-between">
    <a href="/Subcategory/Create" class="btn btn-primary mb-3" onclick="HandlerCRUD(this, event)">
        Create New
    </a>
    <div>
        @{
            if (categories != null)
            {
                <select id="select-category" class="form-select">
                    <option selected>--Tất cả thể loại--</option>
                    @foreach (var item in categories)
                    {
                        <option value="@item.CategoryId">@item.Name</option>
                    }
                </select>
                @section Scripts {
                <script>
                    $(document).ready(function () {
                        $('#select-category').change(function () {
                            var categoryId = $(this).val();
                            // Chỉ thực hiện AJAX nếu một tùy chọn khác "Tất cả thể loại" được chọn
                            if (categoryId) {
                                $.ajax({
                                    url: '/Subcategory/GetSubcategoriesByCategoryID/' + categoryId,
                                    type: 'GET',
                                    success: function (data) {
                                        $('tbody').html(data);
                                    },
                                    error: function (xhr, status, error) {
                                        alert('AJAX Error: ' + status + error)
                                        console.error('AJAX Error: ' + status + error);
                                    }
                                });
                            }
                        });
                    });
                </script>
                }
            }
        }
    </div>
</div>



<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SubcategoryId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Category)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImageUrl)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.SubcategoryId)
                </td>
                <td>
                    @item.Category?.Name
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    <img src="@item.ImageUrl" alt="Image" width="50" height="50" class="object-fit-contain" />
                </td>
                <td>
                    <a href="/Subcategory/Edit" IdValue="@item.SubcategoryId" onclick="HandlerCRUD(this, event)"><i class="bi bi-pencil"></i></a> |
                    <a href="/Subcategory/Delete" IdValue="@item.SubcategoryId" onclick="HandlerCRUD(this, event)"><i class="bi bi-trash3"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table>
