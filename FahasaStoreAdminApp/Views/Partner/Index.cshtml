@using FahasaStoreAPI.Models.EntitiesModels
@model IEnumerable<FahasaStoreAPI.Models.EntitiesModels.PartnerEntities>

@{
    ViewData["Title"] = "Quản lý đối tác";
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["ActiveSidebar"] = 4;
    ViewData["UrlCurrent"] = "/Partner/Index";
    var PartnerTypes = ViewData["PartnerTypes"] as List<PartnerTypeEntities>;
}
<div class="d-flex justify-content-between">
    <a href="/Partner/Create" class="btn btn-primary mb-3" onclick="HandlerCRUD(this, event)">
        Create New
    </a>
    <div>
        @{
            if (PartnerTypes != null)
            {
                <select id="select-PartnerType" class="form-select">
                    <option selected>--Tất cả thể loại--</option>
                    @foreach (var item in PartnerTypes)
                    {
                        <option value="@item.PartnerTypeId">@item.Name</option>
                    }
                </select>
                @section Scripts {
                <script>
                    $(document).ready(function () {
                        $('#select-PartnerType').change(function () {
                            var PartnerID = $(this).val();
                            // Chỉ thực hiện AJAX nếu một tùy chọn khác "Tất cả thể loại" được chọn
                            if (PartnerID) {
                                $.ajax({
                                    url: '/Partner/GetPartnersByType/' + PartnerID,
                                    type: 'GET',
                                    success: function (data) {
                                        $('tbody').html(data);
                                    },
                                    error: function (xhr, status, error) {
                                        alert('AJAX Error: ' + status + error)
                                        console.error('AJAX Error: ' + status + error);
                                    }
                                });
                            }
                        });
                    });
                </script>
                }
            }
        }
    </div>
</div>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PartnerId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PartnerType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImageUrl)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.PartnerId)
                </td>
                <td>
                    @item.PartnerType?.Name
                </td>
                <td>
                    <a href="/Partner/Details" IdValue="@item.PartnerId" onclick="HandlerCRUD(this, event)">@item.Name</a>
                </td>
                <td>
                    <img src="@item.ImageUrl" alt="Image" width="50" height="50" class="object-fit-contain" />
                </td>
                <td>
                    <a href="/Partner/Edit" IdValue="@item.PartnerId" onclick="HandlerCRUD(this, event)"><i class="bi bi-pencil"></i></a> |
                    <a href="/Partner/Delete" IdValue="@item.PartnerId" onclick="HandlerCRUD(this, event)"><i class="bi bi-trash3"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table>
